{
  "name": "get_user_context",
  "description": "Retrieve the current user context including the page they are on, selected menu item, and their profile data from onboarding. Call this at the start of every conversation to personalize responses.",
  "strict": true,
  "parameters": {
    "type": "object",
    "properties": {},
    "additionalProperties": false,
    "required": []
  }
}
